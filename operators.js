// 5 + 5 = 10     // Addition
// 10 - 5 = 5     // Subtraction
// 5 * 10 = 50    // Multiplication
// 10 / 5 = 2     // Division
// 10 % 5 = 0     // Modulo

let n = 4
let m= n++
console.log(m) // post increment is done hence we get the same number as the output 
// first the number gets assogned and then incremented so, the original value got assigned to the m later got incremented 
let a = ++n // this is preincrement that increases the value first and then gets assigned to the a so, when we console it we get
// incremented value of n
console.log(a)

//understand the output by this