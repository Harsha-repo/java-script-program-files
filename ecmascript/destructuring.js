// one onjects or arr data when you eant to assign to some specific variable then we use destructuring

let person = {
    name:'jhon',
    age :25,
    mail:'atrete.com'
}
console.log(person.name)
const {name,age,mail} = person
console.log(name)