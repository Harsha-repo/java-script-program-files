let alien = {
// we use "":"" instead of "=" because we see them as dicts 

    name:"hari",
    tech:'35'
}

console.log(alien,typeof(alien))
console.log(alien.name)
console.log(alien['tech'])
// two ways of accessing the objects using . else [] also allows us to do
